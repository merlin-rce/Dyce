# PlatformIO
.pio/

# VS Code (keep extensions.json, drop the machine-specific bits)
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/

# Secrets - never commit Wi-Fi credentials
src/credentials.h

# Build artefacts
*.bin

# Doc interne (explication du code pour une IA, pas pour le repo)
CODE_EXPLAINED.md
